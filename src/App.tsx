import Button from "@/components/ui/Button/Button";

const App = () => {
  return (
    <div>
      <Button variant="primary" />
      <Button variant="secondary" />
    </div>
  );
};

export default App;
